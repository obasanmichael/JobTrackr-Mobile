/**
 * Limits extreme Dynamic Type scaling so tab chrome and dense layouts remain usable.
 * Primary body copy still scales substantially (WCAG recommends respecting user text settings).
 */
export const TEXT_MAX_FONT_SCALE_MULTIPLIER = 2.25;
